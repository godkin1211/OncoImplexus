% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chromoplexy_detection.R
\name{detect_chromoplexy}
\alias{detect_chromoplexy}
\title{Detect chromoplexy events with corrected graph construction}
\usage{
detect_chromoplexy(
  SV.sample,
  CNV.sample = NULL,
  min_chromosomes = 3,
  min_translocations = 3,
  max_cn_change = 1,
  allow_cycles = TRUE,
  adjacency_distance = 1e+07,
  adjacency_decay_scale = 2e+06,
  deletion_bridge_distance = 1e+06,
  fdr_threshold = 0.01,
  use_statistical_testing = TRUE,
  likely_chromoplexy_threshold = 5,
  possible_chromoplexy_threshold = 4,
  max_path_search = 50,
  max_search_depth = 10,
  max_neighbors = 3,
  max_adjacency_streak = 3,
  genome = "hg19",
  collapse_chains = TRUE,
  collapse_classifications = c("Likely chromoplexy"),
  gene_granges = NULL,
  breakpoint_padding = 1000,
  verbose = TRUE
)
}
\arguments{
\item{SV.sample}{An SVs object}

\item{CNV.sample}{A CNVsegs object (optional)}

\item{min_chromosomes}{Minimum number of chromosomes in a chain (default: 3)}

\item{min_translocations}{Minimum number of translocations in a chain (default: 3)}

\item{max_cn_change}{Maximum allowed copy number change for stability (default: 1)}

\item{allow_cycles}{Whether to allow cycles in detected chains (default: TRUE)}

\item{adjacency_distance}{Maximum distance for genomic adjacency edges (default: 10Mb)}

\item{adjacency_decay_scale}{Distance scale for genomic adjacency weighting (default: 2e6)}

\item{deletion_bridge_distance}{Maximum distance for deletion bridges (default: 1Mb)}

\item{fdr_threshold}{FDR threshold for statistical significance (default: 0.01)}

\item{use_statistical_testing}{Whether to perform statistical testing (default: TRUE)}

\item{likely_chromoplexy_threshold}{Minimum criteria met for "Likely chromoplexy" classification (default: 5)}

\item{possible_chromoplexy_threshold}{Minimum criteria met for "Possible chromoplexy" classification (default: 4)}

\item{max_path_search}{Maximum number of paths to explore (default: 50)}

\item{max_search_depth}{Maximum chain length to explore (default: 10).
Prevents exponential explosion in highly connected graphs.}

\item{max_neighbors}{Maximum number of neighbors to consider per node (default: 3)}

\item{max_adjacency_streak}{Maximum number of consecutive adjacency edges (default: 3)}

\item{genome}{Reference genome for calculating genomic distances (default: "hg19")}

\item{collapse_chains}{Collapse redundant chain enumerations into event-level
connected components (default: TRUE)}

\item{collapse_classifications}{Chain classifications to include in collapsed
events (default: likely chromoplexy only)}

\item{gene_granges}{Optional GRanges object for collapsed event gene annotation}

\item{breakpoint_padding}{Padding in bp around breakpoints for gene annotation}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
A list containing enhanced chromoplexy detection results

A list containing detected chromoplexy chains and summary statistics
}
\description{
This version fixes the critical bug in v2.0 where the translocation graph
was disconnected because it only included translocation edges. The new
implementation adds genomic adjacency edges and deletion bridge edges,
following the ChainFinder methodology.
}
\details{
Key improvements in v3.0:
- Constructs complete graph with genomic adjacency edges
- Integrates deletion bridges as graph edges (not just annotations)
- Properly connects breakpoints on the same chromosome
- Enables detection of complex chromoplexy patterns

IMPORTANT NOTE on Intra-chromosomal SVs:
This function filters to ONLY inter-chromosomal SVs (translocations) for
the primary graph construction, following the ChainFinder methodology.
Intra-chromosomal SVs (DEL, DUP, INV) are NOT explicitly included as edges.
However, their effects are captured through:
1. GENOMIC ADJACENCY edges - connect nearby breakpoints regardless of SV type
2. DELETION BRIDGE edges - deletions between translocation breakpoints
3. CNV analysis - copy number changes are evaluated separately

This approach is biologically justified because chromoplexy is characterized
by chains of TRANSLOCATIONS with minimal local rearrangement. Local
intra-chromosomal events are better captured as adjacency relationships
rather than explicit graph edges.
}
