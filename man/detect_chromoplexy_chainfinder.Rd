% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chromoplexy_chainfinder.R
\name{detect_chromoplexy_chainfinder}
\alias{detect_chromoplexy_chainfinder}
\title{Detect chromoplexy using ChainFinder-style statistical model}
\usage{
detect_chromoplexy_chainfinder(
  SV.sample,
  CNV.sample = NULL,
  min_chromosomes = 3,
  min_translocations = 3,
  adjacency_pvalue_threshold = 0.01,
  adjacency_max_distance = 1e+07,
  deletion_bridge_distance = 1e+06,
  use_chromosome_specific_mu = TRUE,
  max_path_search = 100,
  max_search_depth = 10,
  genome = "hg19",
  verbose = TRUE
)
}
\arguments{
\item{SV.sample}{SV data (data frame or SVs object)}

\item{CNV.sample}{CNV data (data frame or CNVsegs object), optional}

\item{min_chromosomes}{Minimum chromosomes in chain (default: 3)}

\item{min_translocations}{Minimum translocations in chain (default: 3)}

\item{adjacency_pvalue_threshold}{P-value threshold for adjacency (default: 0.01)}

\item{adjacency_max_distance}{Maximum distance to consider (default: 10Mb)}

\item{deletion_bridge_distance}{Distance for deletion bridge search (default: 1Mb)}

\item{use_chromosome_specific_mu}{Use per-chromosome mu (default: TRUE)}

\item{max_path_search}{Maximum paths to search (default: 100)}

\item{max_search_depth}{Maximum chain length (default: 10)}

\item{genome}{Reference genome (default: "hg19")}

\item{verbose}{Print progress (default: TRUE)}
}
\value{
A list containing chromoplexy detection results
}
\description{
This function implements chromoplexy detection following the original
ChainFinder methodology (Baca et al. 2013), which uses statistical testing
to determine whether breakpoint adjacency is significant given the local
background rearrangement rate.
}
